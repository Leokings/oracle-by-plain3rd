{
  "name": "truthfeed-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "TruthFeed static frontend bundled locally with esbuild; no third-party JavaScript CDN is required at runtime.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke:read": "node ../scripts/check_read.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "viem": "2.55.19"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
