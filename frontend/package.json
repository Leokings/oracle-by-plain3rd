{
  "name": "livingconstitution-frontend",
  "private": true,
  "type": "module",
  "description": "LivingConstitution static frontend bundled locally with esbuild; no third-party JavaScript CDN is required at runtime.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "smoke:read": "node ../scripts/check_read.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "viem": "2.55.19"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
