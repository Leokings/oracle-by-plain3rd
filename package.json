{
  "name": "oracle-by-plain3rd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Oracle by Plain3rd unifies TruthFeed evidence decisions and LivingConstitution governance on GenLayer StudioNet.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build",
    "smoke:studionet": "node scripts/studionet-release-smoke.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "2.0.1",
    "@vercel/speed-insights": "2.0.0",
    "viem": "2.55.19"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
