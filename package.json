{
  "name": "oracle-genlayer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Oracle unifies TruthFeed evidence decisions and LivingConstitution governance on GenLayer StudioNet.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "viem": "2.55.19"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}

